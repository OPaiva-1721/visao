"""Telemetria JSONL só com metadados, tempos por etapa para a RN-03 (ADR-009). Fase 1.

- `record.py` — `build_record()`, puro (monta o registro; nunca vê a imagem)
- `writer.py` — `TelemetryWriter`, grava JSONL em disco
"""

"""Watchdog independente: câmera, fps, profundidade, luz (RN-04, RN-27, RN-34). Fase 1.

- `types.py`   — SystemState, FaultKind, SupervisorInputs/State/Report
- `monitor.py` — `tick()`, puro, uma chamada por verificação (não por frame)

Bateria (RN-24) fica para a Fase 5 — não há celular real ainda para medir.
"""

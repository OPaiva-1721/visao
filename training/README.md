# Treino (Fase 4)

Fine-tuning do YOLO para classes fora do COCO (poste, degrau, porta aberta, galho, placa, orelhão…), executado **depois** do teste 3b.

- Treino no Google Colab / Kaggle (sem GPU local).
- Prioridade: datasets públicos prontos (Roboflow Universe, Kaggle); rotular só o que faltar (D7).
- Vídeos e datasets ficam em `data/`, fora do git (ADR-009).
